apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi.fullname" . }}-test-connection"
  labels:
{{ include "multi.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "multi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
